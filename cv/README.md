Put your CV PDF here as `Khadija_Elamri_CV_EN.pdf`.

The download button on the home page links to
`cv/Khadija_Elamri_CV_EN.pdf`, set in `content/profile.json` under
`links.cv`. Rename the file there if you use a different filename.
